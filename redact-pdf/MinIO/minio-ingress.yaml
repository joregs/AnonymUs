apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minio-root-ingress
  namespace: anonymus
spec:
  rules:
    - host: minio-anonymus.kube.isc.heia-fr.ch
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: minio-service
                port:
                  number: 9000
